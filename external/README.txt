The official ReShade SDK headers and ReShade-compatible Dear ImGui headers are installed here by scripts\Setup-All.cmd.
They are deliberately taken from the same ReShade source tree to avoid an ImGui ABI/version mismatch.
